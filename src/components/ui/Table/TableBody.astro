---
import type { HTMLAttributes } from "astro/types";

export interface Props extends HTMLAttributes<"tbody"> {}

const { class: className, ...props } = Astro.props;
---

<tbody class:list={["[&_tr:last-child]:border-0", className]} {...props}>
	<slot />
</tbody>
