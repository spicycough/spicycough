---
import type { HTMLAttributes } from "astro/types";

export interface Props extends HTMLAttributes<"thead"> {}

const { class: className, ...props } = Astro.props;
---

<thead class:list={["[&_tr]:border-b", className]} {...props}>
	<slot />
</thead>
