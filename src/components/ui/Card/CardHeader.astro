---
import type { HTMLAttributes } from "astro/types";

export interface CardHeaderProps extends HTMLAttributes<"div"> {}

const { class: className, ...props } = Astro.props;
---

<div class:list={["flex flex-col space-y-1.5 p-6", className]} {...props}><slot /></div>
